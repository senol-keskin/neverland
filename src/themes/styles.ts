import type { Styles } from '@chakra-ui/theme-tools'

const styles: Styles = {
  global: () => {
    return {
      body: {
        fontFamily: 'body',
      },
    }
  },
}

export default styles
